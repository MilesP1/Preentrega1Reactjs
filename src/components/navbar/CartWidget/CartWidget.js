import Cart from "./Assets/coffee-bold.svg"

const CartWidget = () => {

    return(

    <div>
        <img src={Cart} alt="eventos-proximos" className="image is-64x64 "/> 0
    </div>
    )
}

export default CartWidget;